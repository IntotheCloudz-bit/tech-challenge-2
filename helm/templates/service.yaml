apiVersion: v1
kind: Service
metadata:
  name: tech2-app-service
spec:
  selector:
    app: tech2-app
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  type: {{ .Values.service.type }}